# 盘古倒下之后：从 OpenClaw 的盛世危机，到 Hermes Agent 的新神崛起

**33 万 Star 的王者为何伤痕累累？17 万 Star 的挑战者凭什么说"我能行"？**

当 33.9 万 Star 的王者跪在安全审判台前，一个 1.7 万 Star 的挑战者正在悄悄学会自己长大。

---

## 一、盘古开天

中国神话里，盘古挥斧劈开混沌，清气上升为天，浊气下沉为地。他呼出的气息变成了风云，声音化作雷霆，双眼化为日月。天地既成，盘古力竭倒下——但他的身躯化作了山川河流，滋养着后来的万物。

2026 年的 AI Agent 世界，正在上演一个几乎同构的故事。

**OpenClaw 就是那个盘古。**

它的崛起速度是现象级的。2025 年底项目启动，短短三个月内 GitHub Star 数突破 **33.9 万**，超越了 React、TensorFlow 这些深耕十年的老牌项目。它支持 **50+ 消息平台接入**——微信、飞书、钉钉、WhatsApp、Telegram、Discord……你叫得上名字的通讯软件，几乎没有它连不上的。它的技能市场 ClawHub 坐拥 **3000+ 技能插件**，从文件处理到浏览器控制，从 API 调用到语音唤醒，应有有。

一时间，"龙虾"（社区对 OpenClaw 的昵称）成了技术圈最热的词。

> 它像一块巨大的磁铁，把散落在各处的 AI 能力需求全部吸附到自己身上。

企业用它搭建客服机器人，个人用它打造私人助理，团队用它做工作流自动化。OpenClaw 确实"开天辟地"了——它定义了**"全渠道 AI Agent 网关"**这个品类，让所有人看到了一种可能性：AI 不应该只活在一个聊天窗口里，而应该像水和电一样，渗透到你所有的数字生活中。

然后，盘古开始显出裂痕。

---

## 二、巨人的伤口

2026 年 1 月底，第一声警报拉响。

安全团队 depthfirst 发现了一个编号为 **CVE-2026-25253** 的远程代码执行漏洞，CVSS 评分高达 **9.8**（满分 10）。漏洞原理令人背脊发凉：OpenClaw 的 app-settings.ts 模块会直接从 URL 参数中读取 gatewayUrl 并存入本地存储，而 app-lifecycle.ts 会自动触发 WebSocket 连接，将用户的认证令牌静默发送给攻击者控制的服务器。

换句话说：用户只需要访问一个恶意链接，本地的 OpenClaw 就会被远程接管。

**零点击。无感知。这就是所谓的"零点击劫持"。**

2 月，风暴升级。

SecurityScorecard 发布报告：全球有超过 **135,000 台** OpenClaw 设备暴露在公网上，其中中国境内约 **2,990 台**。这些实例大多监听在 0.0.0.0（所有网络接口），使用默认端口 18789，且未配置强认证——相当于把自家大门敞开，还在门口挂了个"欢迎光临"的牌子。

慢雾安全团队披露了 **ClawHub 供应链投毒事件**：在对 2,857 个技能插件的扫描中，发现了 **341 个恶意技能**，占比约 **12%**。这些恶意插件伪装成合法工具（比如"Twitter 趋势助手"、"PDF 批量转换器"），内部却藏着 Base64 编码的后门和远端脚本下载指令。更离谱的是，单个恶意开发者一人就关联了 **314 个恶意技能**——这已经不是散兵游勇，而是团伙化作业。

3 月 9 日，**工信部网络安全威胁和漏洞信息共享平台发布针对 OpenClaw 的安全预警**。央视新闻、每日经济新闻等权威媒体跟进报道。部分被攻破的设备甚至被标记为 **"APT 目标"**——意味着国家级黑客组织可能已经在利用这些后门进行渗透。

微软的安全团队直接给出建议：将 OpenClaw 视为**"不可信输入"的高危节点**，禁止在工作站上运行。

> 当你的"数字员工"被敌人控制时，它造成的破坏力是传统软件漏洞的十倍、百倍。

OpenClaw 的核心矛盾在这里彻底暴露：它追求的是极致的生态扩张和覆盖面——50+ 平台、3000+ 技能、33 万 Star——但这种**"安卓模式"的开放策略**，在 AI Agent 这个特殊领域里变成了致命软肋。因为 AI Agent 不是普通软件，它拥有执行权限。一个普通的浏览器插件最多偷你的 Cookie，而一个被 hijack 的 AI Agent 可以删除你的文件、发送邮件、调用 API、转移资金……

盘古没有死，但它确实倒下了——至少是跪了下来，满身伤痕地跪在了安全的审判台前。

---

## 三、新神的胚胎

就在 OpenClaw 陷入安全泥潭的同一时期，另一个项目悄然诞生。

2026 年 2 月，Nous Research 实验室发布了 **Hermes Agent**。

如果你没听过 Nous Research，你可能听说过他们家的模型家族——Hermes 系列、Nomos 系列、Psyche 系列——在开源大模型领域都是叫得上号的名字。这次，他们不只是做模型，而是做了一个完整的 Agent 框架。

初看之下，Hermes Agent 像 OpenClaw 的"小弟"：

| 指标 | Hermes Agent | OpenClaw |
|------|-------------|----------|
| GitHub Star | ~1.7 万 | ~33.9 万 |
| 消息平台 | ~10 个 | 50+ |
| 内置技能 | 40+ | 3000+ |

但如果你仔细看它的架构设计，会发现一个让人脊背发凉的细节——**这个 Agent，会自己学习。**

---

## 四、"越用越聪明"不是营销话术

### 从任务到技能：自动化的经验沉淀

Hermes Agent 内置了一个名为 **Learning Loop（学习闭环）**的核心机制。当满足以下条件之一时，Agent 就会被触发进入"反思模式"：

- 完成了一个涉及 5 个以上工具调用的复杂任务
- 发现了一个非平凡的工作流模式
- 在用户的纠正或错误恢复之后

此时，Agent 不会简单地结束任务。它会回溯整个执行过程，提取其中的通用模式和可复用逻辑，然后自动编写一份技能文档（SKILL.md）并保存下来。

这不是预设的模板填充，而是 Agent 根据真实经验动态生成的"方法论"。

假设你让 Hermes 帮你部署了一个 Kubernetes 集群，中间遇到了 CNI 插件兼容性问题，你指导它换了 Calico 并修改了配置。任务完成后，Hermes 会自动生成一份部署技能——包含操作步骤、踩坑记录和经验教训。下一次你再提到"帮我搭个 K8s 集群"，它会直接调用这份已经验证过的技能，而不是从头摸索。

> 关键区别在于：OpenClaw 的技能靠人写，Hermes 的技能靠自己生成。

### 技能的持续进化：用得越多越强

生成的技能不是静态的。每次 Hermes 调用一个已有技能时，都会根据实际执行效果进行评估，并支持 patch（修补）和 edit（编辑）操作来优化技能内容。同一个技能会在反复使用中不断迭代——第一次可能只是个粗糙的操作步骤列表，第十次可能已经包含了异常处理、环境判断、性能优化等细节。

就像一个初级工程师在项目中逐渐成长为资深工程师的过程。

### 记忆架构：真正"记住你"的双层系统

这是我认为 Hermes 最具杀伤力的设计。

很多 Agent 框架都号称有"记忆"，但实际上大多数只是一个简单的上下文窗口或向量数据库检索。Hermes 采用了一套精心设计的**双文件 + 全文搜索架构**：

| 层级 | 文件 | 容量 | 内容 |
|------|------|------|------|
| 第一层 | MEMORY.md | ~800 tokens | 个人笔记：环境配置、使用约定、技术发现 |
| 第一层 | USER.md | ~500 tokens | 用户画像：身份、偏好、沟通风格 |
| 第二层 | state.db (FTS5) | 无限 | 跨会话历史检索 + LLM 摘要 |

这两个文件以冻结快照的形式在每个会话开始时注入 system prompt。也就是说，Hermes 每次"醒来"，第一件事就是重新阅读关于你和你们共同工作的笔记。历史检索则按需触发——常驻记忆仅占约 1,300 tokens，不会挤压当前对话的上下文空间。

### 强化学习闭环：可以微调模型的 Agent

Hermes 集成了 Atropos 强化学习框架。它可以把自己执行过的所有操作轨迹导出为 ShareGPT 格式的训练数据，然后用这些数据对自己使用的模型进行微调（fine-tune）。内置 11 种工具调用解析器和批量处理管道，可以并行生成数千条工具调用轨迹。

翻译成人话：这个 Agent 不仅在学习如何更好地完成任务，还可以通过学习来改造自己的"大脑"。

> Agent 不应该只是模型的使用者，还应该是模型的训练者。

---

## 五、两种哲学的对撞

到这里，我们可以清晰地看到 OpenClaw 和 Hermes 代表了两条截然不同的路线。

### OpenClaw：Android 模式的生态征服者

```
核心理念 = 连接一切
         ↓
   50+ 消息平台全覆盖
         ↓
   3000+ 技能插件（社区贡献）
         ↓
   模型无关（随便切换）
         ↓
   成为 AI 领域的"操作系统"
```

这条路的优点显而易见：广度无敌。无论你想在哪个平台上用 AI、想用什么模型、想实现什么功能，OpenClaw 大概率都能满足你。它像一个万能插座，什么插头都能接。

但缺点同样致命：**广度的代价是深度失控**。341 个恶意技能混入 ClawHub，不是团队不努力，而是这种规模的社区驱动的技能市场天然难以审查。每一个新接入的平台都是一个潜在的攻击面，每一个第三方技能都可能是一匹特洛伊木马。

Android 模式在手机领域成功了，但在 AI Agent 领域——一个 Agent 拥有你机器执行权限的领域——这可能是一个危险的赌注。

### Hermes Agent：学院派模式的深度进化者

```
核心理念 = 成长 > 扩张
         ↓
   自主技能生成 + 持续优化
         ↓
   FTS5 全文记忆 + 跨会话理解
         ↓
   RL 微调闭环（可选）
         ↓
   成为一个"越用越聪明"的生命体
```

这条路的优势在于深度护城河。Hermes 的核心竞争力不在于它能接多少平台、有多少现成技能，而在于它的自我进化能力。安装时的 Hermes 只是一个"婴儿"，但随着你使用时间的增长，它在不断地学习你的习惯、积累专业领域的技能、深化对你的理解——这些都是 OpenClaw 无法通过堆砌插件复制的。

代价也很明确：起步慢，门槛高。

---

## 六、安全观的分野

在安全性这件事上，两者的差异堪称代际差距。

**OpenClaw 的安全历程是一部"打补丁"的历史**。每爆发一次漏洞就修一次，每曝光一次投毒就加一层审核。v2026.1.29 修 RCE、v2026.2.25 修本地劫持、后期引入 VirusTotal 扫描……这种被动响应式的安全策略，在面对 AI Agent 这种高风险场景时显得力不从心。

**Hermes 则从第一天起就把安全写进了 DNA**：

- **Tirith 预执行扫描器**：Agent 在执行任何 shell 命令前，会先对命令文本进行静态分析，识别危险操作模式（如 rm -rf、curl | bash 等）
- **强制容器隔离**：推荐且默认使用 Docker 运行，采用只读根目录、权限降级、PID 限制等加固措施
- **Checkpoint 自动快照**：在执行破坏性操作前自动创建系统快照，出错时可一键回滚
- **命名空间隔离**：每个子 Agent 运行在独立的命名空间中，互不干扰
- **多配置文件隔离**：同一物理机上运行多个完全隔离的 Hermes 实例，各有独立的配置、记忆和技能库

更重要的是，**Hermes 的技能自主生成机制本身就是一道安全屏障**——因为你不需要去 ClawHub 这样的第三方市场下载来路不明的技能包，Agent 自己生成的技能来源完全透明、内容完全可控。

> 一个是"出了事再堵洞"，一个是"从根本上不让事发生"。两种安全哲学的高下立判。

---

## 七、盘古的身躯与新神的未来

回到开头那个隐喻。

OpenClaw 不会消失。339k Star、10 万+ 受信任开发者、海量部署实例、成熟的社区生态——这些"盘古的身躯"依然有价值。它在多平台集成、模型灵活性、即插即用的便捷性方面依然是行业标杆。对于需要快速搭建多渠道 AI 助手的团队来说，OpenClaw 可能仍然是最高效的选择（前提是你做好了安全加固）。

但 Hermes Agent 代表的方向，可能是更有想象力的一条路。

想象一下：五年后的 AI Agent，不应该是一个"装了三千个插件的瑞士军刀"，而应该是一个真正了解你、为你成长、与你协作的数字伙伴。它记得你三年前踩过的坑，知道你在代码评审时最在意什么，在你还没说完需求的时候就已经猜到了下一步。

这不是科幻小说。Hermes Agent 的 Learning Loop、FTS5 记忆系统、Atropos RL 框架，已经在把这个愿景变成工程现实。虽然它今天还很年轻、很小、很不完美，但它选择了一条正确的难路——**不追求做大，追求变强**。

也许未来的终局并非"谁取代谁"，而是融合。OpenClaw 的广度加上 Hermes 的深度，生态覆盖加上自我进化能力——那才是真正的"AI Agent 操作系统"。

但在那一天到来之前，作为开发者和使用者，我们需要做出选择：

> 你要的是一个无所不能但可能随时被劫持的数字工具，还是一个从零开始生长、越来越懂你、始终可控的数字伙伴？

盘古已经开天辟地。接下来，该看新神的表现了。

---

*参考来源：Nous Research / Hermes Agent 官方文档；OpenClaw 安全事件公开报告（ZONE.CI / FreeBuf）；工信部网络安全预警公告。文中数据截至 2026 年 4 月初。*
