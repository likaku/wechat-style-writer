#!/usr/bin/env python3
"""
Hermes Agent vs OpenClaw — 长图 Poster 渲染
文章：盘古倒下之后：从 OpenClaw 的盛世危机，到 Hermes Agent 的新神崛起
"""
import os
import sys

# ── 复制完整引擎代码 ─────────────────────────────────────────────────────
# 引用同目录下的 md_to_image_v2.py 的 CardRenderer
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from md_to_image_v2 import *

# ═══════════════════════════════════════════════════════════════════════════════
#  Hermes vs OpenClaw 文章渲染
# ═══════════════════════════════════════════════════════════════════════════════

# 图片路径（待更新为新生成的统一风格版本）
IMG_COVER = os.path.join(IMG_DIR, "Editorial_cartoon_illustration_2026-04-08T05-39-26.png")  # 封面图
IMG1 = os.path.join(IMG_DIR, "Editorial_cartoon__medieval_fa_2026-04-08T05-41-06.png")       # 盘古开天辟地
IMG2 = os.path.join(IMG_DIR, "Editorial_cartoon_illustration_2026-04-08T05-40-05.png")       # 巨人的伤痕
IMG3 = os.path.join(IMG_DIR, "Editorial_cartoon_illustration_2026-04-08T05-39-50.png")       # 新神的胚胎
IMG4 = os.path.join(IMG_DIR, "Editorial_cartoon__fine_black__2026-04-08T05-42-35.png")       # 越用越聪明闭环
IMG5 = os.path.join(IMG_DIR, "Editorial_cartoon__fine_black__2026-04-08T05-42-16.png")       # 两条路分岔
IMG6 = os.path.join(IMG_DIR, "Fortress_wall_vs_patching_hole_2026-04-07T17-43-02.png")       # 安全观：补洞vs建堡垒
IMG7 = os.path.join(IMG_DIR, "Editorial_cartoon__medieval_fa_2026-04-08T05-41-05.png")       # 新神生长进行时


def main():
    r = CardRenderer(width=750)

    # ═══════════════════════════════════════════════════════════════════
    #  封面
    # ═══════════════════════════════════════════════════════════════════
    r.render_h1("盘古倒下之后")
    r.render_body("**从 OpenClaw 的盛世危机，到 Hermes Agent 的新神崛起**")
    r.y += 4

    r.render_body(
        "在 GitHub 的 Trending 页面上，有一个数字正以不可思议的速度跳动：339,000。"
        "这是 OpenClaw 的 Star 数——一个三个月前还不存在的项目，现在已经把 React 和 TensorFlow 远远甩在身后。"
        "与此同时，在同一个页面的某个角落里，另一个项目安安静静地挂着它的 17,000 颗星，"
        "像一个在图书馆自习的学生，对走廊里的喧嚣充耳不闻。"
    )
    r.render_body(
        "这两个数字之间的故事，是一个关于开天辟地的故事，也是一个关于遗产继承的故事。"
    )

    # 封面图：盘古与新神的全景
    r.render_image(IMG_COVER, caption="盘古倒下之后，新神在他的遗产上生长")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第一章：开天
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(1, "开天")

    r.render_body(
        "中国神话里，盘古挥斧劈开混沌。清气上升为天，浊气下沉为地，"
        "他的呼吸变成了风云，声音化作雷霆，双眼化为日月。"
        "然后，盘古力竭倒下。但他的身躯化作了山川河流，滋养着后来的万物。"
    )
    r.render_body("2026 年的 AI Agent 世界里，有人正在扮演这个角色。")
    r.render_body("**OpenClaw 就是那个盘古。**")

    r.render_body(
        "它做对了一件事：让所有人看到，AI 不应该只活在一个聊天窗口里。"
        "微信、飞书、钉钉、WhatsApp、Telegram、Discord——你能叫出名字的通讯软件，它几乎都能接入。"
        "它的技能市场 ClawHub 坐拥三千多个插件，从文件处理到浏览器控制，从 API 调用到语音唤醒。"
        '社区给它起了个昵称叫"龙虾"，这个名字一度成了技术圈最热的词。'
    )

    r.render_body(
        '它确实开天辟地了。它定义了"全渠道 AI Agent 网关"这个品类，'
        "证明了一种可能性——AI 可以像水和电一样，渗透到你所有的数字生活中。"
    )

    r.render_body("然后，裂痕出现了。")

    # 插图 1：盘古劈开混沌
    r.render_image(IMG1, caption="盘古开天辟地——一斧劈开一个时代")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第二章：伤痕
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(2, "伤痕")

    r.render_body("2026 年 1 月底，第一声警报拉响。")

    r.render_body(
        "安全团队 depthfirst 发现了一个编号为 CVE-2026-25253 的远程代码执行漏洞，"
        "CVSS 评分 9.8，满分 10。漏洞的原理简单得令人不安：OpenClaw 会直接从 URL 参数中读取服务器地址并存入本地，"
        "然后自动把你的认证令牌发送过去。用户只需要点击一个链接。不需要任何其他操作。"
    )

    r.render_body('**零点击。无感知。**安全研究者们管这叫"零点击劫持"。')

    r.render_body("二月，事情变得更糟。")

    r.render_body(
        "SecurityScorecard 的报告显示，全球有超过十三万五千台 OpenClaw 设备暴露在公网上。"
        "这些实例大多监听在 0.0.0.0——所有网络接口——使用默认端口，没有配置认证。"
        '用一个不太优雅的比喻：这相当于把自家大门敞开，还在门口竖了块\u201c欢迎光临\u201d的牌子。'
    )

    r.render_body(
        "更令人不安的消息来自供应链。慢雾安全团队扫描了 ClawHub 上近三千个技能插件，"
        "发现其中三百四十一个是恶意的——占比约百分之十二。"
        "一个开发者账号关联了三百一十四个恶意技能。这不是散兵游勇。这是流水线作业。"
    )

    r.render_body(
        "三月九日，工信部发布了正式的安全预警。央视跟进报道。"
        "部分被攻破的设备被标记为 APT 目标——意味着国家级攻击者可能已经在利用这些后门。"
    )

    r.render_callout(
        '当一个拥有执行权限的\u201c数字员工\u201d被敌人接管时，它能造成的破坏，不是传统软件漏洞可以相提并论的。'
    )

    r.render_body(
        "OpenClaw 的核心矛盾在这里暴露无遗。它追求的是覆盖面——五十个平台、三千个技能、三十三万颗星——"
        '但这种"安卓模式"的开放策略，在 AI Agent 这个特殊领域里，成了阿喀琉斯之踵。'
    )

    r.render_body("盘古没有死。但它跪下了。")

    # 插图 2：受伤的巨人
    r.render_image(IMG2, caption="带着伤痕跪下的先驱者")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第三章：胚胎
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(3, "胚胎")

    r.render_body(
        "就在 OpenClaw 焦头烂额地修补城墙的时候，几条街之外，一间安静的实验室里，有人在做一件不同的事。"
    )
    r.render_body("2026 年 2 月，Nous Research 发布了 **Hermes Agent**。")
    r.render_body(
        "Nous Research 不是无名之辈。他们的开源模型家族——Hermes、Nomos、Psyche——在圈内都叫得响。"
        "但这次他们没有发布模型，而是做了一个完整的 Agent 框架。"
    )

    r.render_body(
        "初看之下，这个框架的数据不太好看：一万七千颗星，十来个消息平台，四十多个内置技能。"
        "和 OpenClaw 放在一起，像大学城里一家刚开业的独立书店，隔壁是 Costco。"
    )

    # 数据对比
    r.y += 6
    r.render_data_row('GitHub Star', '~1.7 万 vs 33.9 万', highlight=True)
    r.render_data_row('消息平台', '~10 vs 50+', highlight=True)
    r.render_data_row('内置技能', '40+ vs 3,000+', highlight=True)
    r.y += 6

    r.render_body("但如果你打开它的架构文档，往深处看，会发现一个让人脊背发凉的细节。")
    r.render_callout("这个 Agent，会自己学习。")

    # 插图 3：安静的实验室
    r.render_image(IMG3, caption="独立书店里的小学徒——会自己长大的 Agent")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第四章：学习
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(4, "学习")

    r.render_body(
        "Hermes Agent 内部有一个叫 **Learning Loop** 的机制。"
        "当它完成了一个足够复杂的任务——比如涉及五个以上工具调用的操作，"
        "或者你中途纠正了它的错误——它不会简单地收工。"
        "它会停下来，回溯整个执行过程，提取其中可复用的逻辑，然后给自己写一份操作手册。"
    )
    r.render_body("不是模板填充。是它根据亲身经历写出来的方法论。")

    r.render_body(
        "假设你让它部署了一个 Kubernetes 集群，中间遇到了 CNI 插件的兼容性问题，你指导它换了 Calico。"
        "任务完成后，Hermes 会把这次经历编写成一份部署技能——包含步骤、踩坑记录、经验教训。"
        '下次你说"帮我搭个 K8s"，它会直接调用这份已验证的技能，而不是从头摸索。'
    )

    r.render_callout("OpenClaw 的技能靠人写。Hermes 的技能靠自己长出来。")

    r.render_body(
        "更有趣的是，这些技能不是静态的。每次被调用时，Hermes 都会根据实际效果评估并修订。"
        "第一次可能只是个粗糙的步骤列表；第十次可能已经包含了异常处理、环境判断和性能优化。"
        "就像一个初级工程师在真实项目中慢慢变成了老手。"
    )

    r.render_h3("记忆")
    r.render_body(
        '很多 Agent 框架都说自己有记忆功能，但实际上大多是一个向量数据库加上关键词检索。'
        "Hermes 的做法不一样。它维护两份核心档案：一份关于你的工作环境和偏好（MEMORY.md），"
        '一份关于你这个人（USER.md）。每次\u201c醒来\u201d，它做的第一件事就是重新阅读这两份笔记。'
    )

    items_mem = [
        ('MEMORY.md (~800 tokens)', '工作环境、使用约定、技术发现'),
        ('USER.md (~500 tokens)', '身份、偏好、沟通风格'),
        ('state.db (FTS5)', '跨会话历史检索 + LLM 摘要'),
    ]
    for t, d in items_mem:
        r.render_bullet(f"**{t}**：{d}")
    r.y += 4

    r.render_body(
        "常驻记忆只占约一千三百个 token——轻到几乎不存在，但足够让它记住该记住的一切。"
    )

    r.render_h3("强化学习：改造自己的大脑")
    r.render_body(
        "最后一层：它集成了强化学习框架 Atropos，可以把自己的操作轨迹导出为训练数据，用来微调自己使用的模型。"
    )
    r.render_callout("这个 Agent 不只是在学习怎么做事。它在学习怎么做一个更好的自己。")

    # 插图 4：自我进化
    r.render_image(IMG4, caption="镜中的自己——越用越聪明的闭环")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第五章：两条路
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(5, "两条路")

    r.render_h3("安卓的路")
    r.render_body(
        "OpenClaw 走的是连接一切、覆盖一切的路，让生态自己繁荣。"
        "它像一个万能插座——什么插头都能接，什么电器都能用。"
        "优点是广度无敌。缺点是你无法确保每一个接进来的东西都是安全的。"
    )
    r.render_body(
        "三百四十一个恶意插件不是因为团队不努力，而是这种模式的结构性代价。"
        "在手机领域，安卓的开放策略最终胜出了。但在 AI Agent 领域——"
        "一个代理程序拥有你机器执行权限的领域——赌注要大得多。"
    )

    r.render_h3("山路")
    r.render_body(
        "Hermes 走的是另一条路。不追求接入多少平台，不在乎技能市场有多热闹。"
        "它押注的是深度：自己生成技能，自己迭代技能，自己记住用户，自己改进大脑。"
        '装机时它只是个婴儿，但随着时间推移，它会变成一个越来越了解你、越来越难以替代的伙伴。'
    )
    r.render_body("代价也很明确：起步慢，门槛高，不够性感。")

    r.render_quote("一条是热闹的大路。一条是安静的山路。")

    # 插图 5：分岔的路
    r.render_image(IMG5, caption="热闹的大路 vs 安静的山路")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第六章：两堵墙
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(6, "两堵墙")

    r.render_body("安全这件事，或许最能说明两种哲学的差距。")
    r.render_body(
        "OpenClaw 的安全史是一部紧急修补手册。一月修远程代码执行，二月修本地劫持，"
        "后来加上 VirusTotal 扫描。每一次修补都是对的、必要的——"
        "但这种被动响应的节奏，在 AI Agent 这种高风险场景里，总是慢了一步。"
    )
    r.render_body("**Hermes 从第一天起就做了不同的选择。**")

    sec_items = [
        ('Tirith 预执行扫描器', '任何 shell 命令运行前做静态分析'),
        ('强制容器隔离', 'Docker 运行，只读根目录，权限降级'),
        ('Checkpoint 自动快照', '破坏性操作前自动创建快照，出错一键回滚'),
        ('命名空间隔离', '每个子 Agent 独立运行空间，互不干扰'),
    ]
    for t, d in sec_items:
        r.render_bullet(f"**{t}**：{d}")

    r.y += 4
    r.render_body(
        "而且，因为 Hermes 的技能是自己生成的，你根本不需要去第三方市场下载来路不明的插件。"
        "攻击面从源头上就被收窄了。"
    )

    r.render_callout(
        "一个在修城墙上的洞。一个从一开始就把城墙建得更厚。"
    )

    # 插图 6：补洞 vs 建堡垒
    r.render_image(IMG6, caption="补洞 vs 建堡垒——两种安全哲学")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第七章：遗产
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(7, "遗产")

    r.render_body(
        "OpenClaw 不会消失。三十三万颗星、十万开发者、成熟的社区生态——这些都是真实的价值。"
        '更重要的是，它作为先驱者，证明了"全渠道 AI Agent 网关"是一个真实的、巨大的需求。'
        "它探索了五十个平台接入的可能性边界，建立了技能市场的运作范式。"
        "所有后来者——包括 Hermes——都在它开辟的土地上行走。"
    )
    r.render_body("但 Hermes 代表的方向，或许更值得注意。")
    r.render_body(
        "想象一下五年后的 AI Agent。"
        '它不应该是一个装了三千个插件的瑞士军刀，而应该是一个真正了解你的伙伴——'
        "记得你三年前踩过的坑，知道你在代码评审时最在意什么，"
        "在你还没说完需求的时候就猜到了下一步。"
    )
    r.render_body(
        "Hermes 的 Learning Loop、FTS5 记忆系统、Atropos RL 框架，"
        "已经在把这个愿景变成工程现实。"
    )
    r.render_body(
        "也许最终的答案不是非此即彼。"
        "OpenClaw 铺下的广度路基，加上 Hermes 探索的深度进化——那才是真正的下一代。"
    )

    r.render_callout(
        "你要的是一个无所不能但随时可能被劫持的工具，"
        "还是一个从零开始生长、越来越懂你、始终在你掌控之中的伙伴？"
    )

    r.render_body("盘古已经倒下。他的身躯化作了山川。")
    r.render_body("接下来，看新神的了。")

    # 插图 7：新神生长进行时
    r.render_image(IMG7, caption="新神生长进行时——路，是一起铺的")

    # ── Footer ─────────────────────────────────────────────────────────
    r.render_footer("盘古倒下之后 · Kaku")

    # ── 保存 ────────────────────────────────────────────────────────────
    out = os.path.join(os.path.dirname(os.path.abspath(__file__)), "hermes_poster.png")
    r.save(out)


if __name__ == "__main__":
    main()
