#!/usr/bin/env python3
"""
Hermes Agent vs OpenClaw — 长图 Poster 渲染
文章：盘古倒下之后：从 OpenClaw 的盛世危机，到 Hermes Agent 的新神崛起
"""
import os
import sys

# ── 复制完整引擎代码 ─────────────────────────────────────────────────────
# 引用同目录下的 md_to_image_v2.py 的 CardRenderer
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from md_to_image_v2 import *

# ═══════════════════════════════════════════════════════════════════════════════
#  Hermes vs OpenClaw 文章渲染
# ═══════════════════════════════════════════════════════════════════════════════

IMG = os.path.join(IMG_DIR, "New_Yorker_style_cartoon__A_co_2026-04-07T12-49-42.png")       # 盘古开天辟地
IMG2 = os.path.join(IMG_DIR, "New_Yorker_style_satirical_car_2026-04-07T11-36-08.png")   # 巨人的伤痕
IMG3 = os.path.join(IMG_DIR, "Baby_robot_learning_cartoon_2026-04-07T17-55-36.png")     # 新神的胚胎
IMG4 = os.path.join(IMG_DIR, "New_Yorker_style_satirical_car_2026-04-07T11-37-46.png")  # 越用越聪明闭环
IMG5 = os.path.join(IMG_DIR, "Diverging_roads_cartoon_2026-04-07T18-11-43.png")         # 两条路分岔
IMG6 = os.path.join(IMG_DIR, "Fortress_wall_vs_patching_hole_2026-04-07T17-43-02.png")  # 安全观：补洞vs建堡垒


def main():
    r = CardRenderer(width=750)

    # ═══════════════════════════════════════════════════════════════════
    #  封面
    # ═══════════════════════════════════════════════════════════════════
    r.render_h1("盘古的遗产")
    r.render_body("**从 OpenClaw 的盛世与伤痕，到 Hermes Agent 的新路探索**")
    r.y += 4
    r.render_body('33 万 Star 的先驱者留下了什么？1.7 万 Star 的后来者在思考什么？')
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第一章：盘古开天
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(1, "盘古开天")

    r.render_body(
        "中国神话里，盘古挥斧劈开混沌，清气上升为天，浊气下沉为地。"
        "他呼出的气息变成了风云，声音化作雷霆，双眼化为日月。"
        "天地既成，盘古力竭倒下——但他的身躯化作了山川河流，滋养着后来的万物。"
    )
    r.render_body("2026 年的 AI Agent 世界，正在上演一个几乎同构的故事。")
    r.render_body("**OpenClaw 就是那个盘古。**")

    r.render_body(
        "它的崛起速度是现象级的。2025 年底项目启动，短短三个月内 GitHub Star 数突破 **33.9 万**，"
        "超越了 React、TensorFlow 这些深耕十年的老牌项目。它支持 **50+ 消息平台接入**——"
        "微信、飞书、钉钉、WhatsApp、Telegram、Discord……你叫得上名字的通讯软件，几乎没有它连不上的。"
        "它的技能市场 ClawHub 坐拥 **3000+ 技能插件**，从文件处理到浏览器控制，从 API 调用到语音唤醒，应有尽有。"
    )

    r.render_body('一时间，㌌龙虾㌍（社区对 OpenClaw 的昵称）成了技术圈最热的词。')

    r.render_callout(
        "它像一块巨大的磁铁，把散落在各处的 AI 能力需求全部吸附到自己身上。"
    )

    r.render_body(
        'OpenClaw 确实「开天辟地」了——它定义了**「全渠道 AI Agent 网关」**这个品类，'
        "让所有人看到了一种可能性：AI 不应该只活在一个聊天窗口里，而应该像水和电一样，渗透到你所有的数字生活中。"
    )

    r.render_body("然后，盘古开始显出疲惫。")

    # 插图 1：盘古劈开混沌
    r.render_image(IMG, caption="盘古开天辟地，身躯化作山川")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第二章：巨人的伤痕
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(2, "巨人的伤痕")

    r.render_body("2026 年 1 月底，第一声警报拉响。")

    r.render_body(
        "安全团队 depthfirst 发现了一个编号为 **CVE-2026-25253** 的远程代码执行漏洞，"
        "CVSS 评分高达 **9.8**（满分 10）。漏洞原理令人背脊发凉："
        "OpenClaw 的 app-settings.ts 模块会直接从 URL 参数中读取 gatewayUrl 并存入本地存储，"
        "而 app-lifecycle.ts 会自动触发 WebSocket 连接，将用户的认证令牌静默发送给攻击者控制的服务器。"
    )

    r.render_body("换句话说：用户只需要访问一个恶意链接，本地的 OpenClaw 就会被远程接管。")
    r.render_body('**零点击。无感知。这就是所谓的㌌零点击劫持㌍。**')

    r.render_body("2 月，风暴升级。")

    r.render_body(
        "SecurityScorecard 发布报告：全球有超过 **135,000 台** OpenClaw 设备暴露在公网上，其中中国境内约 **2,990 台**。"
        "这些实例大多监听在 0.0.0.0（所有网络接口），使用默认端口 18789，且未配置强认证——"
        '相当于把自家大门敞开，还在门口挂了个㌌欢迎光临㌍的牌子。'
    )

    r.render_body(
        "慢雾安全团队披露了 **ClawHub 供应链投毒事件**：在对 2,857 个技能插件的扫描中，发现了 **341 个恶意技能**，占比约 **12%**。"
        "更离谱的是，单个恶意开发者一人就关联了 **314 个恶意技能**——这已经不是散兵游勇，而是团伙化作业。"
    )

    r.render_body(
        "**3 月 9 日，工信部网络安全威胁和漏洞信息共享平台发布针对 OpenClaw 的安全预警**。"
        '央视新闻、每日经济新闻等权威媒体跟进报道。部分被攻破的设备甚至被标记为 **㌌APT 目标㌍**。'
    )

    r.render_callout(
        '当你的㌌数字员工㌍被敌人控制时，它造成的破坏力是传统软件漏洞的十倍、百倍。'
    )

    r.render_body(
        "OpenClaw 的核心矛盾在这里彻底暴露：它追求的是极致的生态扩张和覆盖面——"
        '50+ 平台、3000+ 技能、33 万 Star——但这种**㌌安卓模式㌍的开放策略**，'
        "在 AI Agent 这个特殊领域里变成了致命软肋。因为 AI Agent 不是普通软件，它拥有执行权限。"
    )

    r.render_quote(
        "盘古没有倒下。它只是跪了下来，满身伤痕地站在安全的审判台前——而它的身躯，正在化作山川河流。"
    )

    # 插图 2：受伤的巨人跪在审判台前
    r.render_image(IMG2, caption="33万Star的先驱者，带着伤痕继续前行")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第三章：新神的胚胎
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(3, "新神的胚胎")

    r.render_body(
        "就在 OpenClaw 陷入安全泥潭的同一时期，另一个项目悄然诞生。"
        "2026 年 2 月，Nous Research 实验室发布了 **Hermes Agent**。"
    )
    r.render_body(
        "如果你没听过 Nous Research，你可能听说过他们家的模型家族——Hermes 系列、Nomos 系列、Psyche 系列——"
        "在开源大模型领域都是叫得上号的名字。这次，他们不只是做模型，而是做了一个完整的 Agent 框架。"
    )
    r.render_body('初看之下，Hermes Agent 像 OpenClaw 的㌌小弟㌍：')

    # 数据对比表（用 data_row）
    r.y += 6
    r.render_data_row('GitHub Star', '~1.7 万', highlight=True)
    r.render_data_row('消息平台', '~10 个', highlight=True)
    r.render_data_row('内置技能', '40+', highlight=True)
    r.render_data_row('', '(OpenClaw: 33.9万 / 50+ / 3000+)')
    r.y += 6

    r.render_body("但如果你仔细看它的架构设计，会发现一个让人脊背发凉的细节——")
    r.render_callout("这个 Agent，会自己学习。")

    # 插图 3：婴儿机器人学习
    r.render_image(IMG3, caption="新神的胚胎——会自己长大的 Agent")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第四章："越用越聪明"不是营销话术
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(4, '㌌越用越聪明㌍不是营销话术')

    r.render_h3("从任务到技能：自动化的经验沉淀")
    r.render_body(
        "Hermes Agent 内置了一个名为 **Learning Loop（学习闭环）**的核心机制。"
        '当完成复杂任务、发现非平凡工作流模式、或在用户纠正后，Agent 就会被触发进入㌌反思模式㌍。'
    )
    r.render_body(
        "此时，Agent 会回溯整个执行过程，提取通用模式和可复用逻辑，然后自动编写一份技能文档（SKILL.md）并保存下来。"
        '这不是预设的模板填充，而是 Agent 根据真实经验动态生成的㌌方法论㌍。'
    )
    r.render_callout(
        "关键区别在于：OpenClaw 的技能靠人写，Hermes 的技能靠自己生成。"
    )

    r.render_h3("技能的持续进化：用得越多越强")
    r.render_body(
        "每次 Hermes 调用一个已有技能时，都会根据实际执行效果进行评估，并支持 patch 和 edit 操作来优化技能内容。"
        "同一个技能会在反复使用中不断迭代——第一次可能只是个粗糙的操作步骤列表，第十次可能已经包含了异常处理、环境判断、性能优化等细节。"
    )
    r.render_quote("就像一个初级工程师在项目中逐渐成长为资深工程师的过程。")

    r.render_h3('记忆架构：真正㌌记住你㌍的双层系统')
    r.render_body(
        '很多 Agent 框架都号称有㌌记忆㌍，但大多数只是一个简单的上下文窗口或向量数据库检索。'
        "Hermes 采用了一套精心设计的**双文件 + 全文搜索架构**："
    )

    items_mem = [
        ('MEMORY.md (~800 tokens)', '个人笔记：环境配置、使用约定、技术发现'),
        ('USER.md (~500 tokens)', '用户画像：身份、偏好、沟通风格'),
        ('state.db (FTS5)', '跨会话历史检索 + LLM 摘要'),
    ]
    for t, d in items_mem:
        r.render_bullet(f"**{t}**：{d}")
    r.y += 4

    r.render_body(
        "这两个文件以冻结快照的形式在每个会话开始时注入 system prompt。"
        "常驻记忆仅占约 1,300 tokens，不会挤压当前对话的上下文空间；历史检索则按需触发。"
    )

    r.render_h3("强化学习闭环：可以微调模型的 Agent")
    r.render_body(
        "Hermes 集成了 Atropos 强化学习框架。它可以把自己执行过的所有操作轨迹导出为 ShareGPT 格式的训练数据，"
        "然后用这些数据对自己使用的模型进行微调（fine-tune）。内置 11 种工具调用解析器和批量处理管道。"
    )
    r.render_callout("Agent 不应该只是模型的使用者，还应该是模型的训练者。")

    # 插图 4：自我进化的机器人
    r.render_image(IMG4, caption="镜像中的自己——越用越聪明的闭环")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第五章：两种哲学的对撞
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(5, "两种哲学的对撞")

    r.render_h3('OpenClaw：Android 模式的生态征服者')
    r.render_body(
        '核心理念 = 连接一切 → 50+ 消息平台全覆盖 → 3000+ 技能插件（社区贡献）→ 模型无关 → 成为 AI 领域的㌌操作系统㌍'
    )
    r.render_body(
        "优点显而易见：广度无敌。无论你想在哪个平台上用 AI、想用什么模型、想实现什么功能，"
        "OpenClaw 大概率都能满足你。它像一个万能插座，什么插头都能接。"
    )
    r.render_body(
        "但缺点同样致命：**广度的代价是深度失控**。341 个恶意技能混入 ClawHub，"
        "这种规模的社区驱动的技能市场天然难以审查。每一个新接入的平台都是一个潜在的攻击面，"
        "每一个第三方技能都可能是一匹特洛伊木马。"
    )
    r.render_quote(
        "Android 模式在手机领域成功了，但在 AI Agent 领域——一个 Agent 拥有你机器执行权限的领域——这可能是一个危险的赌注。"
    )

    r.render_h3("Hermes Agent：学院派模式的深度进化者")
    r.render_body(
        '核心理念 = 成长 > 扩张 → 自主技能生成 + 持续优化 → FTS5 全文记忆 + 跨会话理解 → RL 微调 → 成为一个㌌越用越聪明㌍的生命体'
    )
    r.render_body(
        "这条路的优势在于深度护城河。Hermes 的核心竞争力不在于它能接多少平台、有多少现成技能，"
        '而在于它的自我进化能力。安装时的 Hermes 只是一个㌌婴儿㌍，但随着使用时间的增长，'
        "它在不断学习你的习惯、积累专业领域的技能、深化对你的理解——这些是 OpenClaw 无法通过堆砌插件复制的。"
    )
    r.render_body("代价也很明确：起步慢，门槛高。")

    # 插图 5：两条分岔的路
    r.render_image(IMG5, caption="两条路：Android 广度 vs 学院派深度")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第六章：安全观的分野
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(6, "安全观的分野")

    r.render_body("在安全性这件事上，两者的差异堪称代际差距。")
    r.render_body(
        '**OpenClaw 的安全历程是一部㌌打补丁㌍的历史**。'
        '每爆发一次漏洞就修一次，每曝光一次投毒就加一层审核。'
        "v2026.1.29 修 RCE、v2026.2.25 修本地劫持、后期引入 VirusTotal 扫描……"
        "这种被动响应式的安全策略，在面对 AI Agent 这种高风险场景时显得力不从心。"
    )
    r.render_body("**Hermes 则从第一天起就把安全写进了 DNA**：")

    sec_items = [
        ('Tirith 预执行扫描器', '执行 shell 命令前静态分析危险操作模式'),
        ('强制容器隔离', 'Docker 运行，只读根目录、权限降级、PID 限制'),
        ('Checkpoint 自动快照', '破坏性操作前创建系统快照，出错可一键回滚'),
        ('命名空间隔离', '每个子 Agent 独立运行空间，互不干扰'),
    ]
    for t, d in sec_items:
        r.render_bullet(f"**{t}**：{d}")

    r.y += 4
    r.render_callout(
        '一个是「出了事再堵洞」，一个是「从根本上不让事发生」。两种安全哲学的高下立判。'
    )

    # 插图 6：补洞 vs 建堡垒
    r.render_image(IMG6, caption="补洞 vs 建堡垒——两种安全哲学")
    r.render_separator()

    # ═══════════════════════════════════════════════════════════════════
    #  第七章：盘古的身躯与新神的未来
    # ═══════════════════════════════════════════════════════════════════
    r.render_h2(7, "盘古的身躯与新神的未来")

    r.render_body(
        "OpenClaw 不会消失。339k Star、10 万+ 受信任开发者、海量部署实例、成熟的社区生态——"
        '这些㌌盘古的身躯㌍依然有价值。更重要的是，它作为**先驱者**，为整个 AI Agent 行业奠定了不可磨灭的理论基础。'
        "它证明了「全渠道 AI Agent 网关」这个品类是真实存在的、有巨大需求的。"
        "它探索了 50+ 平台接入的可能性边界。它建立了技能市场和社区生态的运作范式——这些都是后来者的路基。"
    )
    r.render_body(
        "但 Hermes Agent 代表的方向，是在这条路上走得更深。"
    )
    r.render_body(
        '想象一下：五年后的 AI Agent，不应该是一个㌌装了三千个插件的瑞士军刀㌍，'
        "而应该是一个真正了解你、为你成长、与你协作的数字伙伴。"
        "它记得你三年前踩过的坑，知道你在代码评审时最在意什么，在你还没说完需求的时候就已经猜到了下一步。"
    )
    r.render_body(
        "这不是科幻小说。Hermes Agent 的 Learning Loop、FTS5 记忆系统、Atropos RL 框架，已经在把这个愿景变成工程现实。"
        "虽然它今天还很年轻、很小、很不完美，但它选择了一条正确的难路——**不追求做大，追求变强**。"
    )
    r.render_body(
        '也许未来的终局并非㌌谁取代谁㌍，而是融合与继承。'
        'OpenClaw 铺下的广度路基 + Hermes 探索的深度进化 = 下一代真正的㌌AI Agent 操作系统㌍。'
    )

    r.render_callout(
        "你要的是一个无所不能但可能随时被劫持的数字工具，还是一个从零开始生长、越来越懂你、始终可控的数字伙伴？"
        "或者更可能的是——两者都是必经之路。"
    )

    r.render_body("盘古已经开天辟地，身躯化作山川。接下来，该看新神如何在这片土地上生长了。")

    # ── Footer ─────────────────────────────────────────────────────────
    r.render_footer("盘古的遗产 · 新神生长进行时")

    # ── 保存 ────────────────────────────────────────────────────────────
    out = os.path.join(os.path.dirname(os.path.abspath(__file__)), "hermes_poster.png")
    r.save(out)


if __name__ == "__main__":
    main()
