# 2026-04-07 Daily Log

## 安装 skill：mck-speech-design-skill

- 来源：https://github.com/likaku/mck-speech-design-skill
- 安装路径：~/.agents/skills/mck-speech-design-skill（symlink 到 CodeBuddy）
- 功能：根据 PPT/文档生成演讲稿，支持 27 种风格（Musk、马云、乔布斯等），自动注入 PPTX 备注
- 触发词：演讲稿、发言稿、讲稿、备注、speaking notes、keynote 等

## PPT 备注填充（Elon Musk 风格 · 90分钟版）

- 在原版基础上用 Elon Musk 风格重写（第一性原理 × 极客冷幽默 × 平淡开场+愿景高潮）
- 输出文件：AI_Agent_Coding_McKinsey_Style_V2_musk_90min.pptx（~/Downloads/）
- 时长控制：每页约2.5分钟，总计约9400字

## PPT 备注填充：AI_Agent_Coding_McKinsey_Style_V2.pptx

- 工具：mck-speech-design-skill（来自 github.com/likaku/mck-speech-design-skill）+ inject_notes.py
- 内容来源：微信_20260403_114159(1).docx（演讲转写文本 + AI 总结纪要）
- 结果：35 页全部注入 ✅，输出文件 AI_Agent_Coding_McKinsey_Style_V2_with_notes.pptx（~/Downloads/）
- 工作流：读取 PPT 结构 → 匹配 Word 内容生成 JSON → inject_notes.py 注入 → 原地验证

## 用 md2wechat + wechat-style-writer 生成微信文章

- 文章主题：Skill 替代 App（深度科技分析长文）
- 工具链：md2wechat v2.0.7（Homebrew 安装）+ wechat-style-writer skill
- md2wechat API 模式需要 MD2WECHAT_API_KEY（未配置），AI 模式返回 prompt 需外部大模型处理
- 最终用 wechat-style-writer 的 md_to_wechat.py（jinjiju 主题）+ md_to_word.py 生成 HTML 和 Word
- 输出目录：~/wechat-output/skill-replace-app/
- 生成文件：article.html（微信 HTML）、article.docx（Word 可直接导入公众号）、article.md、封面图
- 封面图用 image_gen 生成（黑色重墨水插图风格）

## md_to_image v2 细长版 + 讽刺漫画插图
- 升级宽度到 **750px**（更细长，适合手机阅读），输出 **750×4617px** 长图
- 新增 `render_image()` 方法支持在文字间插入插图（自适应宽度+caption）
- 新增 `render_data_row()` 方法用于展示数据对比行（标签+数值右对齐）
- 用用户之前的 **Skill替代App** 完整原文作为测试内容（5章节）
- 用 AI 生成了 **3 张 New Yorker 风格讽刺漫画**作为章节间插图：
  - 图1: 手机监狱+SKILL钥匙 / 图2: 乌龟vs火箭 / 图3: 6个手机的人 vs Done的人
- 文件：`md_to_image_v2.py`、输出：`skill_vs_app_full.png`
- **修复 render_image bug**：原代码用全局变量 `MARGIN_X` 而非 `self.MX`，且 `_ensure_height` 扩展画布后未重建 `self.draw` 对象，导致第2、3张图粘贴失败。修复后 3 张插图全部正常渲染。
- **新增 kaku 签名**：每张插图右下角加灰色小字 "kaku" 签名（F_CAPTION 字体，#B4B4B4 颜色）
- **升级为 5 张完整插图版**：新增图4（一面墙APP vs 一句话搞定）、图5（躺椅上的人说"Book me a flight to Tokyo"），输出 **750×5600px** 完整长图
- **签名改草书字体**：使用 `NothingYouCouldDo-Regular.ttf` 手写体作为 kaku 签名字体
- **纯白背景 + 字号+32%**：背景从 (248,248,246) 改为纯白 (255,255,255)，Callout 底色改为 (250,250,249)，所有字号放大约 32%（H1:34→45, 正文:16→22），输出 **750×7420px**
- **行间距统一优化**：引入三级间距常量 GAP_S(10)/GAP_M(16)/GAP_L(24) 统一所有元素段后间距；render_paragraph_with_inline 增加 `after` 参数；Callout/Quote 统一用 LINE_HEIGHT 计算行距；修复 "当天全球软件股市" 行间距异常问题

## 公众号图片化输出方案 — 原型验证（md_to_image）
- 用户提出新需求：将公众号文章通过**图片方式**输出，实现格式和字体100%可控
- 核心思路：Markdown → PIL 渲染为图片卡片 → 嵌入Word → 导入公众号
- 基于**风格一（Chromatic Silence）**设计语言开发了 md_to_image_proto.py 最小原型
- 已验证能力：H1中文混排标题、H2/H3章节、正文自动换行、**加粗**行内渲染、Callout金句块、引用块、列表、分隔线、动态高度自适应
- 技术要点：CJK/ASCII分段混排渲染（split_text_by_script + draw_mixed_text）、行内Markdown解析（parse_inline）、动态画布扩容
- 字体方案：中文 PingFang SC + 英文 BricolageGrotesque/JetBrainsMono
- 输出：1080×1346px PNG 卡片（proto_card_v01.png），效果验证通过

## WorkBuddy 宣传海报生成（canvas-design skill）

- 使用 canvas-design skill 生成三款 WorkBuddy 宣传海报（3:4 / 900×1200px）
- 风格一：Chromatic Silence（极简主义·科技冷感，白底+深蓝+电子蓝绿）
- 风格二：Neon Archaeology（赛博朋克·数字律动，深色底+霓虹绿电路风）
- 风格三：Warm Systems（有机温暖协作，象牙底+琥珀色+梅紫）
- 中文字体：PingFang SC（苹方），英文字体：BricolageGrotesque/Tektur/YoungSerif 等
- 输出文件：workbuddy_poster_style1/2/3.png（工作目录）
- 核心宣传点：全场景AI智能体、非技术用户友好、15000+内测用户、一句话描述需求

## 更新 wechat-style-writer SKILL.md — 新增长图 Poster 标准输出格式（完整）

- 将 `md_to_image_v2.py` 的所有设计标准整合进 wechat-style-writer skill
- 新增 **输出格式 D: 长图/Poster**（D.1-D.9 完整设计规范）：
  - D.1 设计哲学（极简白底、+32%字号、呼吸感间距、kaku签名）
  - D.2 画布参数（750px宽、40px边距、1.75行高）
  - D.3 配色系统（10色纯白背景版色板）
  - D.4 字体系统（9种字体变量，含字号和用途说明）
  - D.5 三级间距系统 GAP_S(10)/GAP_M(16)/GAP_L(24) + 各方法 after 参数规范
  - D.6 元素渲染规范（H1/H2/H3/body/callout/quote/bullet/separator/image/data_row/footer 的 ASCII 图解+参数）
  - D.7 插图风格（New Yorker 讽刺漫画 + 配图数量指南）
  - D.8 渲染引擎代码结构（CardRenderer 类完整 API）
  - D.9 输出流程（5步：写作→配图→脚本→渲染→交付）
- 流程 C 从 5 步升级为 6 步，新增 Step 5（长图渲染）
- 默认输出从「MD + Word」升级为 **「MD + Word + PNG长图」三格式必出**
- scripts 目录新增 `md_to_image_v2.py` 说明（通用模板版）
- 关键约定新增第 3/8/9 条（长图路径、D规范遵循、三格式默认输出）
- 图片生成规范新增「长图 Poster 配图」子节

### 引擎脚本集成到 skill

- 将 `md_to_image_v2.py` 复制到 `~/.workbuddy/skills/wechat-style-writer/scripts/md_to_image_v2.py`
- **重构 main() 为通用模板**：移除硬编码的 Skill vs App 文章内容，替换为：
  - 完整的 API 速查表（10 个 render 方法 + 用法说明）
  - 典型公众号长文结构示例（封面→章节→结尾，含注释掉的 image 调用）
  - 可配置的 OUTPUT_PATH 和 IMG_DIR
  - 使用方法说明（4步：复制→改路径→写内容→运行）
- 原始硬编码版本保留在工作目录 `/Users/kaku/WorkBuddy/20260407113700/md_to_image_v2.py`
- 未来使用方式：**复制 skill 脚本 → 改 main() → 运行**

## 推送到 GitHub

- 创建公开仓库：**https://github.com/likaku/wechat-style-writer**
- 提交信息：feat: wechat-style-writer 长图 Poster 渲染引擎 v2 + 风格库
- 23 个文件，2678 行代码

## 安装 skill：md2wechat

- 安装来源：https://github.com/geekjourneyx/md2wechat-skill
- 安装路径：~/.agents/skills/md2wechat（symlink 到 CodeBuddy）
- 安全审查：Gen Safe，Socket 0 alerts，Snyk Med Risk（可接受）
- 功能：Markdown 转微信公众号 HTML，支持草稿上传、AI 写作、封面生成、小绿书等
- 触发词：公众号、微信文章、md2wechat、Markdown 转换、草稿发布等

## Hermes Agent vs OpenClaw 文章长图渲染完成 ✅

- **文章**：`hermes_article.md`（7章完整深度分析，~4000字中文长文）
- **渲染脚本**：`md_to_image_hermes.py`（基于 md_to_image_v2.py 引擎）
- **修复的 Bug**（共 4 处）：
  1. L277 中文引号 `"` 导致 SyntaxError → 替换为 `「」`
  2. L146-149 `render_data_row()` 参数错误（单字符串→需 label+value 两参数）
  3. L191-193 `items_mem` 元组拆分错误（含 `㌌, ㌍` 分隔符导致3值拆包）
  4. L267-270 `sec_items` 同样的元组拆分错误
- **插图**：6 张 New Yorker 风格讽刺漫画全部嵌入（IMG1-IMG6）
- **输出**：`hermes_poster.png` — **750×12291px** 完整长图海报
- 文章主题：OpenClaw（33万Star）的安全危机 vs Hermes Agent（1.7万Star）的自我进化能力
